# ⚠️ 這是**公開** repo，只負責發布。規則是「預設全擋，逐檔放行」——
# 因為兩份練習頁都是單檔自足的（題目連正解與解析全部內嵌在 HTML 裡），
# 只要不小心讓明文進來，GitHub Pages 就會把整份題庫公開出去。
#
# 2026-08-18 起這裡**只放三種東西**：
#   1. index.html            選擇入口頁（不含題目，不需要密碼）
#   2. AZ*/xxx-practice.html **加密後**的練習頁
#   3. AZ*/README.md         說明
#
# 題庫明文、SPEC、全部工具都在另一個**私有** repo。
# 理由：GitHub Pages 在個人帳號上沒有任何存取控制（官方：「即使 repo 是 private，
# Pages 網站預設仍然公開在網際網路上」），而免費方案下 repo 一轉私有 Pages 會直接
# 停用。唯一可行的是內容自己加密——而那要求明文不能待在同一個公開 repo，
# 否則點進 repo 就繞過密碼了。
#
# ❌ 不要放行 banks/、tools/、*-SPEC.md，或未加密的練習頁。

*
!/.gitignore
!/README.md
!/index.html

!/AZ900/
/AZ900/*
!/AZ900/README.md
!/AZ900/az900-practice.html

!/AZ104/
/AZ104/*
!/AZ104/README.md
!/AZ104/az104-practice.html
